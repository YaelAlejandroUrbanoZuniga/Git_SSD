{
  "name": "ssd-pipeline-management",
  "version": "1.0.0",
  "description": "SSD Pipeline Management System — Nexteer Automotive",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "echo 'Run dev:frontend or dev:backend separately'",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build"
  },
  "devDependencies": {}
}
